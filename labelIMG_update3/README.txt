——————————更新方法——————————
更新1&2
**用压缩包里的文件替换掉原来文件

更新3：
**直接用就行了，用法和原标注工具一样

——————————更新日志——————————

更新1：
**修复了打开文件不能打开同文件夹下xml标记文件的bug

更新2：
**现在打开一个图片时可以同时打开同文件夹下的多个xml了，每个xml前缀均与图片同名，后面加上不同后缀
**软件内会显示所有xml里的标注

更新3：
**现在打开一个图片时会检测有没有名称完全一样的对应xml文件，如果有就只打开这一个文件，如果没有会打开所有前缀与图片同名的xml文件并显示所有标注
**不同文件的标注现会用不同颜色的bounding box在图片中显示出来了（至多三种）

**标注规范：每一个人标注的时候用相同的类名，但是保存xml时后面加上自己名字的拼音缩写；最后校对人员校对时保存为与图片相同名字的文件（最终标注文件）。